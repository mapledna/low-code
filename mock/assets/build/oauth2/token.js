/* 登录请求【第二个】
特征：POST请求。没有返回信息（可能是被拦截了？）

API地址：api/build/oauth2/token
参数：{ token: { appCode: 【appCode】 } }
*/

const parameter = { token: { appCode: 'prod' } }

const result = null

export default result
